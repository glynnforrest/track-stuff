{% extends 'track-stuff:base.html.twig' %}

{% block content %}
  <div class="row panel">
    <div class="columns small-12">
      {% for goal in goals %}
        <a href="{{ route('track-stuff:goal:view', {id: goal.id}) }}">
          <h2>
            {{ goal.title }}
          </h2>
        </a>
        <div class="progress round">
          <span class="meter" style="width: {{goal.percentage}}%">
          </span>
        </div>
        {{ goal.total }} {{ goal.slug }}
      {% endfor %}
    </div>
  </div>
{% endblock %}
